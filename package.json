{
  "name": "@mnicoledev/erezo-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for the eRezo API — manage contacts and events from Claude",
  "type": "module",
  "bin": {
    "erezo-mcp-server": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --out-dir dist --clean",
    "dev": "tsx src/index.ts"
  },
  "keywords": [
    "mcp",
    "erezo",
    "model-context-protocol",
    "contacts",
    "events",
    "networking"
  ],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "tsup": "^8.4.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0"
  }
}
